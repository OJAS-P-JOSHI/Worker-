# Conversion to YAML from Properties formar report
# Warnings:
# - The yaml file had comments which are lost in the refactoring!
spring:
  application:
    name: learningapp
  datasource:
    url: jdbc:mysql://localhost:3306/LEARNING_APP
    username: root
    password: Stunner79.
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: ojaspjoshi1729@gmail.com
    password: yoyw yrsv dxrm znyc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
